html
  body
    form(action="/charge", method="post")
      article
        p Press the button below to make a payment via STRIPE secure payment system for:
        label Amount: #{labelAmount} #{denomination}
      script(
        src="//checkout.stripe.com/v2/checkout.js",
        class="stripe-button",
        data-key=keyPublishable,
        data-locale="auto",
        data-description="Sample Charge",
        data-amount= amount
        )
