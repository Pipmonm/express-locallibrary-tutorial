extends layout

block content
  h1(href="#" style={'font-size':'4vw'})= title
  br

  h3(href="#" style={'font-size':'3vw'})= "NOTE CAREFULLY"

  p!= themeDesc1

  p!= themeDesc2
  br
  <div style="text-align: center">
  a(href=source)
             button(type="button") download FracSpellerDual (exe file)
  <div class="divider"/>
  a(href='/catalog/downloadUSBFS_view')
           button(type='button') USB Version
  </div>
  br
  br
  a(href='/catalog/verification')
           button(type='button') Verify Download
  //- note that dashed comments (ie this one) are only for PUG (not the transcribed HTML )
  //- the PUG href requires two lines, gives errors otherwise
  br
  br
