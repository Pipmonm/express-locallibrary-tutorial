extends layout

block content
  h1= title

  ul
  each clientrequest in clientrequest_list
    li
      a(href=clientrequest.url) #{clientrequest.status}
      |  (#{clientrequest.client.name})
      |  (#{clientrequest.client.email_address})

  else
    li There are no clientrequests.
